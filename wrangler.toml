name = "kfc-chat-bot"
main = "worker.js"
compatibility_date = "2024-11-30"

[[kv_namespaces]]
binding = "TOPIC_MAP"
id = "19ebaf503e9947e38b13f85aadff1d48"

# 注意：KV 命名空间 ID 每个用户都不一样，
# 所以不能写死在这里。用户需要在 Cloudflare 后台手动绑定。
